// src/pages/project/proposal/ProposalEditor.tsx
// Placeholder: the real editor lands in Task 11. The route exists now so the
// proposals list can navigate to a proposal without a dead link.
import React from 'react';

export const ProposalEditor: React.FC = () => null;
